{
  "type": "postgres",
  "host": "localhost",
  "port": 5423,
  "username": "postgres",
  "password": "docker",
  "database": "findpartner",
  "entities": [
      "./src/models/*.ts"
  ],
  "migrations": [
      "./src/database/migrations/*.ts"
  ],
  "cli": {
      "migrationsDir": "./src/database/migrations"
  }
}
